// import { selectPatientAccountingPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectPatientAccountingPageDomain();

describe('selectPatientAccountingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
